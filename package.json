{
  "name": "escape-room-quiz",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm -w server run dev\" \"npm -w client run dev\"",
    "build": "npm -w server run build && npm -w client run build",
    "start": "npm -w server run start",
    "lint": "npm -w client run lint && npm -w server run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
