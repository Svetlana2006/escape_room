# Local deploy settings for `npm run deploy`
# Copy to `.env.deploy` and fill in your GitHub token.

DEPLOY_GITHUB_TOKEN=github_pat_here
GITHUB_DEPLOY_REF=main
GITHUB_DEPLOY_WORKFLOW=deploy-pages-render.yml
